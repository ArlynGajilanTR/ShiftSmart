# ShiftSmart Test Environment Configuration
# This file contains test-specific configuration

# Test Claude API (using cheaper model for cost savings)
# Note: You need to set a real API key for live tests
ANTHROPIC_TEST_API_KEY=sk-ant-test-xxx
TEST_MODEL=claude-3-haiku-20240307  # Cheapest model for tests

# Test database (separate from development)
TEST_DATABASE_URL=postgresql://test_user:test_pass@localhost:5432/shiftsmart_test

# Test limits to prevent runaway costs
MAX_TEST_TOKENS=1000
TEST_TIMEOUT_MS=30000
TEST_MAX_RETRIES=2

# Mock server configuration
MOCK_CLAUDE_PORT=3001
MOCK_CLAUDE_DELAY_MS=500

# Test user credentials
TEST_ADMIN_EMAIL=test.admin@thomsonreuters.com
TEST_ADMIN_PASSWORD=testadmin123
TEST_USER_EMAIL=test.user@thomsonreuters.com
TEST_USER_PASSWORD=testuser123

# Feature flags for testing
ENABLE_LIVE_AI_TESTS=false  # Set to true for nightly runs
ENABLE_PERFORMANCE_TESTS=false  # Set to true for weekly runs
